import 'package:flutter/material.dart';

const barColor = Color(0xFF411414);
const scaffoldColor = Color(0xFF201F48);

const colorWhite = Colors.white;
const colorGrey = Color(0x60FFFFFF);
const colorGreen = Colors.green;
const colorRed = Colors.red;

const cardBackground = Color(0xFF424567);
const tabActive = Colors.blue;

const cardShadow = BoxShadow(
  color: Colors.black26,
  spreadRadius: 3,
  blurRadius: 5,
  offset: Offset(0, 3),
);

const rocketScreenText = TextStyle(
  color: colorWhite,
  fontSize: 13,
  fontWeight: FontWeight.w300,
);
